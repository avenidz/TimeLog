<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_graph_drawer.xml"
    app:startDestination="@id/drawerHome">

    <fragment
        android:id="@+id/drawerHome"
        android:name="com.example.timelog.fragment.FragmentHome"
        android:label="Home"
        tools:layout="@layout/fragment_home" />
    <fragment
        android:id="@+id/drawerSettings"
        android:name="com.example.timelog.fragment.FragmentSettings"
        android:label="Settings"
        tools:layout="@layout/fragment_settings" />
    <fragment
        android:id="@+id/drawerLogout"
        android:name="com.example.timelog.fragment.FragmentLogout"
        android:label="Logout"
        tools:layout="@layout/fragment_logout" />
</navigation>